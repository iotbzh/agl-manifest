rm -rf /xdt/build/tmp/

prepare_meta -f iotbzh -o /xdt -l ~/mirror/ -t porter -p ~/mirror/proprietary-renesas-r-car/ -e wipeconfig "$@"
echo 'INHERIT += "rm_work"' >>/xdt/build/conf/local.conf
. /xdt/build/agl-init-build-env
bitbake agl-demo-platform-crosssdk

prepare_meta -f iotbzh -o /xdt -l ~/mirror/ -t raspberrypi3 -e wipeconfig "$@"
echo 'INHERIT += "rm_work"' >>/xdt/build/conf/local.conf
. /xdt/build/agl-init-build-env
bitbake agl-demo-platform-crosssdk

prepare_meta -f iotbzh -o /xdt -l ~/mirror/ -t intel-corei7-64 -e wipeconfig "$@"
echo 'INHERIT += "rm_work"' >>/xdt/build/conf/local.conf
. /xdt/build/agl-init-build-env
bitbake agl-demo-platform-crosssdk

